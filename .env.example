# Supabase (Phase 2A — identity & commercial truth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# CashPlan email reminders (Resend — https://resend.com). Sent to the signed-in user’s email.
RESEND_API_KEY=
# Optional; default is Resend onboarding sender for testing
# CASHPLAN_REMINDER_FROM_EMAIL=CashPlan <onboarding@resend.dev>
