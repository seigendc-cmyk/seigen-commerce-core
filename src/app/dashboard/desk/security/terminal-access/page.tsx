import { TerminalAccessConsole } from "@/modules/terminal/ui/terminal-access-console";

export const metadata = { title: "Terminal access" };

export default function TerminalAccessPage() {
  return <TerminalAccessConsole />;
}
